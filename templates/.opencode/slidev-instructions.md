# Slidev Project

Slide content is in `slides.md`. Official Slidev skill provides full syntax reference.

## Commands

- `npm run dev` — Dev server at http://localhost:3030
- `npm run build` — Single self-contained HTML to `dist/`
